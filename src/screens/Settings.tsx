import React, { useState } from "react"

interface Settings {
    columns: number;
    colourScheme: string;
    impossibleMode: boolean;
    startImpossibleMode: boolean;
    open: boolean;
    close: boolean;
    boardWidth: number;
    width: number;
    height: number;
    updateSettings: void;
}

export const Settings = () => {

}